'use client';
import { useEffect,useState } from 'react';
import { useStore } from './context';
export function TrackingConsent(){const {data}=useStore();const [choice,setChoice]=useState<string|null>('pending');const enabled=!!(data.settings.ga_id||data.settings.meta_pixel_id);useEffect(()=>setChoice(localStorage.getItem('paw_analytics_consent')),[]);useEffect(()=>{if(choice!=='accepted'||!enabled)return;const w=window as any;const id=data.settings.ga_id;if(id&&!document.getElementById('paw-ga')){w.dataLayer=w.dataLayer||[];w.gtag=function(){w.dataLayer.push(arguments);};w.gtag('js',new Date());w.gtag('config',id);const s=document.createElement('script');s.id='paw-ga';s.async=true;s.src='https://www.googletagmanager.com/gtag/js?id='+encodeURIComponent(id);document.head.appendChild(s);}const pixel=data.settings.meta_pixel_id;if(pixel&&!document.getElementById('paw-meta')){const fb:any=function(){fb.callMethod?fb.callMethod.apply(fb,arguments):fb.queue.push(arguments);};fb.queue=[];fb.loaded=true;fb.version='2.0';w.fbq=fb;const script=document.createElement('script');script.id='paw-meta';script.async=true;script.src='https://connect.facebook.net/en_US/fbevents.js';document.head.appendChild(script);fb('init',pixel);fb('track','PageView');}},[choice,enabled,data.settings.ga_id,data.settings.meta_pixel_id]);if(!enabled||choice)return null;return <div className="cookie-consent"><p>Allow optional analytics to help improve the store? <a href="/privacy">Privacy information</a></p><button className="btn outline" onClick={()=>{localStorage.setItem('paw_analytics_consent','declined');setChoice('declined');}}>Decline</button><button className="btn" onClick={()=>{localStorage.setItem('paw_analytics_consent','accepted');setChoice('accepted');}}>Allow analytics</button></div>;}
